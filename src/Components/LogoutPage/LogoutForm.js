const LogoutForm = () => {

    // Somewhat good-looking bootstrap template for logging
    return (
        <div id="centering_id">
            <p>Logout</p>
        </div>
    );
};

export default LogoutForm;