function LoginForm() {
    //Gdy zalogowany <Navigate replace to="/controlPanel" />

    return (
        <div className="LoginForm">
            LOGIN
        </div>
    );
}

export default LoginForm;